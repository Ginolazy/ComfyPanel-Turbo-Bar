{
  "name": "comfypanel-turbo-bar",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build:frontend": "vite build src --outDir ../dist --emptyOutDir",
    "build": "tauri build",
    "tauri": "tauri",
    "build:dmg": "python3 build_turbo_bar.py"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^7.1.5"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}